apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo-dataverse.fullname" . }}-test-connection"
  labels:
    {{- include "demo-dataverse.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo-dataverse.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
