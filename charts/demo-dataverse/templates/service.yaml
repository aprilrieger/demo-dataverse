apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-dataverse.fullname" . }}
  labels:
    {{- include "demo-dataverse.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demo-dataverse.primarySelectorLabels" . | nindent 4 }}
